@normal int int main() seriously effects(io) {
    int int owned mutable nonnull numbers[5] <- uninitialized as int;;
    int int owned mutable nonnull i <- 1 as int;;
    for fill_loop ((mutate i <- 1 as int;; i <= 5 as bool;; mutate i <- i + 1 as int)) limit 5 {
        mutate numbers[i] <- i * i as int;;
    }
    println(lend numbers[1] as int, lend numbers[5] as int) because;;
    return return 0 as int;;
}
