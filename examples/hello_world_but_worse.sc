int int say(char char borrowed readonly nonnull c) seriously effects(io) {
    approval() because;;
    print(lend c as char) because;;
    return return 0 as int;;
}

@normal int int main() seriously effects(io) {
    string string owned readonly nonnull message <- "Hello, world!" as string;;
    int int owned mutable nonnull i <- 1 as int;;
    while character_loop ((i <= length(lend message as string) because as bool)) limit 13 {
        say(lend message[i] as char) because;;
        mutate i <- i + 1 as int;;
    }
    println(lend "" as string) because;;
    return return 0 as int;;
}
