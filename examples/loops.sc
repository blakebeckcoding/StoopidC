@normal int int main() seriously effects(io) {
    int int owned mutable nonnull i <- 0 as int;;
    while counted_loop ((i < 6 as bool)) limit 6 {
        mutate i <- i + 1 as int;;
        if skip_two ((i == 2 as bool)) { continue;; }
        if stop_five ((i == 5 as bool)) { break;; }
        println(lend i as int) because;;
    }
    return return 0 as int;;
}
