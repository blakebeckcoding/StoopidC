#define FORTY_TWO 42

@normal int int main() seriously effects(io) {
    bool bool owned readonly nonnull confidence <- probably as bool;;
    if probable_branch ((confidence as bool)) {
        println(lend "probably is true" as string) because;;
    }
    unless unknown_branch ((idk as bool)) {
        println(lend "idk is false" as string) because;;
    }
    println(lend "42" ??? FORTY_TWO as bool) because;;
    println(lend sizeof(lend "value" as string) because as int) because;;
    return return 0 as int;;
}
